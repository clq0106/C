#include <stdio.h>

#define KEY 'X';

int main() {
	char key, ciphertext[32], newciphertext;

	ciphertext = 0;
	key = KEY;
	printf("Geben Sie einen text ein welchen SIe übersetzen wollen\n");
	scanf("%s", &ciphertext);
	free(ciphertext);

	for(i != NULL; 


	return 0;
}
